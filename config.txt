COMANDES IMPORTANTS
sudo apt update
sudo apt install apache2
192.168.1.148
sudo a2enmod cgid
sudo systemctl restart apache2
sudo chmod 755 /usr/lib/cgi-bin/test.cgi
%d delete all lines on vim
sudo useradd -m guest_user -p passwd1

sudo chmod -R 755 /usr/lib/cgi-bin
sudo mv $HOME/* /usr/lib/cgi-bin

visudo
add sudoers file the bash scripts that needs sudo password (/etc/sudoers)
www-data ALL=(ALL:ALL) ALL

sudo touch /etc/cron.allow
sudo echo "www-data" >> /etc/cron.allow
sudo touch /var/spool/cron/www-data
